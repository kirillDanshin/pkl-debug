amends "./generic_env.pkl"

env = "local"

resources {
    resourceA {
        endpoint = "http://localhost:42069"
        fullResourceName = this.getFullResourceName()
    }

    resourceB {
        endpoint = "http://localhost:42024"
        fullResourceName = this.getFullResourceName()
    }
}

